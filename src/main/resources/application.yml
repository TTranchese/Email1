spring.sendgrid.api-key: "SENDGRID_API_KEY_TO_GENERATE"
spring:
  mail:
    host: smtp.sendgrid.net
    port: 587
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
